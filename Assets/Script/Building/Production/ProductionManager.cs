using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProductionManager : MonoBehaviour
{
    /*
    [Header("Building Count")]
    [SerializeField] private List<ProductionController> _productionCount = new List<ProductionController>();

    public List<ProductionController> ProductionCount
    {
        get => _productionCount;
        set
        {
            _productionCount = value;
        }
    }
    */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
